"use client";

import Link from "next/link";
import { useState } from "react";

import { useContentModule } from "@/components/content/use-content-module";
import { kanaRomajiLabel, kanaRows, toKatakana, type KanaScript } from "@/lib/kana";
import type { KanjiItem } from "@/lib/types";

const kanaGroups = [
  { label: "Gojūon · basic sounds", rows: kanaRows.slice(0, 10) },
  { label: "Dakuten · handakuten", rows: kanaRows.slice(10, 15) },
  { label: "Yōon · blended sounds", rows: kanaRows.slice(15) },
];

export function ReferenceCharts({ kanji }: Readonly<{ kanji: KanjiItem[] }>) {
  const module = useContentModule();
  const [script, setScript] = useState<KanaScript>("hiragana");
  const uniqueKanji = [...new Map((module.kanji.length ? module.kanji : kanji).map((item) => [item.character, item])).values()];

  return <div className="space-y-8"><section className="surface-panel p-6 sm:p-8"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="eyebrow mb-2">Kana · かな</p><h2 className="jp-serif text-3xl text-[#f5f5f2]">Keep the shapes nearby.</h2><p className="mt-2 max-w-xl text-sm leading-6 text-[#9297a1]">Use this shelf when you need a reminder. The active drill stays answer-only so the chart cannot give the recall away.</p></div><div className="flex gap-2"><button type="button" onClick={() => setScript("hiragana")} className={`rounded-lg px-3 py-2 text-xs ${script === "hiragana" ? "bg-[#e5b85c] text-[#0b0b0d]" : "border border-[#3f4652] text-[#9297a1]"}`}>ひらがな</button><button type="button" onClick={() => setScript("katakana")} className={`rounded-lg px-3 py-2 text-xs ${script === "katakana" ? "bg-[#e5b85c] text-[#0b0b0d]" : "border border-[#3f4652] text-[#9297a1]"}`}>カタカナ</button></div></div><div className="mt-7 space-y-6">{kanaGroups.map((group) => <div key={group.label}><div className="mb-3 flex items-center justify-between"><p className="text-xs font-semibold uppercase tracking-[.16em] text-[#e5b85c]">{group.label}</p><span className="text-xs text-[#676c75]">{group.rows.flatMap(([characters]) => [...characters]).length} sounds</span></div><div className="grid gap-2 sm:grid-cols-2 lg:grid-cols-3">{group.rows.map(([characters, sounds]) => <div key={characters} className="rounded-xl border border-white/10 bg-[#101b2b]/70 p-2"><p className="mb-2 px-1 text-[10px] uppercase tracking-[.14em] text-[#676c75]">row</p><div className="grid grid-cols-5 gap-1">{sounds.map((sound, index) => { const kana = [...characters][index]; const displayKana = script === "hiragana" ? kana : toKatakana(kana); return <div key={displayKana} className="rounded-lg bg-[#17181d]/90 px-1 py-2 text-center"><p className="jp-serif text-2xl text-[#f5f5f2]">{displayKana}</p><p className="mt-1 text-[10px] text-[#e5b85c]">{kanaRomajiLabel(displayKana, sound)}</p></div>; })}</div></div>)}</div></div>)}</div><div className="mt-7 flex flex-wrap gap-3"><Link href="/practice/kana" className="rounded-xl bg-[#e34a3f] px-4 py-3 text-sm font-semibold text-[#0b0b0d]">Practice kana →</Link><span className="self-center text-xs text-[#9297a1]">The expanded rows include ぎ, づ (du), ぱ, and blended sounds.</span></div></section><section className="surface-panel p-6 sm:p-8"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="eyebrow mb-2">Kanji · 漢字</p><h2 className="jp-serif text-3xl text-[#f5f5f2]">A first shelf of characters.</h2><p className="mt-2 max-w-xl text-sm leading-6 text-[#9297a1]">Start with the characters in the N5 curriculum, then open any card for connected words and practice.</p></div><Link href="/practice?mode=kanji" className="text-sm text-[#e5b85c] hover:text-[#f1cf7c]">Kanji drills →</Link></div><div className="mt-7 grid grid-cols-2 gap-2 sm:grid-cols-3 lg:grid-cols-5">{uniqueKanji.map((item) => <Link key={item.id} href={`/entry/${item.id}`} className="group rounded-xl border border-white/10 bg-[#101b2b]/70 p-3 transition hover:-translate-y-0.5 hover:border-[#e5b85c]/60"><p className="text-center"><ruby className="jp-serif text-4xl text-[#f5f5f2]">{item.character}<rt className="font-sans text-[10px] text-[#e5b85c]">{item.kunyomi[0] ?? item.onyomi[0] ?? ""}</rt></ruby></p><p className="mt-3 truncate text-center text-xs text-[#c3c7ce]">{item.meanings[0]}</p><p className="mt-1 truncate text-center text-[10px] text-[#676c75]">{item.usefulWords[0]?.word ?? "N5 character"}</p></Link>)}</div></section></div>;
}
