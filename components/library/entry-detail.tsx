"use client";

import Link from "next/link";

import { JapaneseText } from "@/components/learning/japanese-text";
import { GrammarVisual } from "@/components/learning/grammar-visual";
import { ReadingPanel } from "@/components/learning/reading-panel";
import { ItemNote } from "@/components/library/item-note";
import { LearnerAssistant } from "@/components/learning/learner-assistant";
import { SaveSentence } from "@/components/library/save-sentence";
import { StudyLaterButton } from "@/components/library/study-later";
import { n5Module } from "@/lib/curriculum";
import { classifyItem } from "@/lib/jlpt";
import { toHiragana } from "@/lib/mastery";
import type { GrammarContrast, KanjiItem, ListeningItem, ReadingItem, VocabularyItem, GrammarItem } from "@/lib/types";

type Entry = VocabularyItem | KanjiItem | GrammarItem | ReadingItem | ListeningItem;

function VocabularyDetail({ item, vocabulary, kanji }: Readonly<{ item: VocabularyItem; vocabulary: VocabularyItem[]; kanji: KanjiItem[] }>) {
  const frequency = typeof item.frequency === "number" ? item.frequency.toLocaleString() : "";
  const perMillion = typeof item.frequencyMetadata?.pmw === "number" ? item.frequencyMetadata.pmw.toFixed(2) : "";
  return <><p className="jp-serif text-6xl text-[#f5f5f2]"><ruby>{item.writtenForm}<rt className="text-lg text-[#e5b85c]">{toHiragana(item.reading)}</rt></ruby></p><p className="mt-5 text-lg text-[#f5f5f2]">{item.meanings.join(" · ")}</p><p className="mt-2 text-sm text-[#9297a1]">{item.partOfSpeech} · {item.collocations.join(" · ")}</p>{frequency || perMillion ? <p className="mt-3 text-xs text-[#676c75]">Written frequency · {frequency || "—"}{perMillion ? ` · ${perMillion} per million` : ""}</p> : null}<div className="mt-8 space-y-3">{item.exampleSentences.map((example) => <div key={example.japanese} className="rounded-xl bg-[#101b2b]/70 p-4"><p className="jp-serif text-lg text-[#e5b85c]"><JapaneseText text={example.japanese} vocabulary={vocabulary} kanji={kanji} /></p><p className="mt-1 text-sm text-[#9297a1]">{example.translation}</p><SaveSentence sourceItemId={item.id} japanese={example.japanese} translation={example.translation} /></div>)}</div></>;
}

function KanjiDetail({ item, vocabulary }: Readonly<{ item: KanjiItem; vocabulary: VocabularyItem[] }>) {
  const metadata = [item.strokeCount ? `${item.strokeCount} strokes` : "", item.grade ? `school grade ${item.grade}` : "", item.radical ? `radical ${item.radical}` : ""].filter(Boolean);
  return <><p className="jp-serif text-8xl text-[#f5f5f2]"><ruby>{item.character}<rt className="text-lg text-[#e5b85c]">{toHiragana(item.kunyomi[0] ?? item.onyomi[0] ?? "")}</rt></ruby></p><p className="mt-5 text-lg text-[#f5f5f2]">{item.meanings.join(" · ")}</p><p className="mt-3 text-sm text-[#9297a1]">On: {item.onyomi.map(toHiragana).join(" · ") || "—"} · Kun: {item.kunyomi.map(toHiragana).join(" · ") || "—"}</p>{metadata.length || item.nanori?.length || item.components?.length || item.mnemonic || item.strokeOrder ? <div className="mt-5 rounded-xl border border-white/10 bg-[#101b2b]/60 p-4"><p className="eyebrow mb-2">Kanji study data</p>{metadata.length ? <p className="text-sm text-[#c3c7ce]">{metadata.join(" · ")}</p> : null}{item.nanori?.length ? <p className="mt-2 text-xs text-[#9297a1]">Name readings: {item.nanori.map(toHiragana).join(" · ")}</p> : null}{item.components?.length ? <p className="mt-2 text-xs text-[#9297a1]">Components: {item.components.join(" · ")}</p> : null}{item.mnemonic ? <p className="mt-3 text-sm leading-6 text-[#c3a998]">{item.mnemonic}</p> : null}{item.strokeOrder ? <p className="mt-3 text-xs text-[#9297a1]">Stroke order: {item.strokeOrder}</p> : null}</div> : null}<div className="mt-8"><p className="eyebrow mb-3">Useful words</p><div className="grid gap-3 sm:grid-cols-2">{item.usefulWords.map((word) => { const linked = vocabulary.find((entry) => entry.writtenForm === word.word); const content = <><p className="jp-serif text-xl text-[#e5b85c]"><ruby>{word.word}<rt className="text-xs text-[#e5b85c]">{toHiragana(word.reading)}</rt></ruby></p><p className="mt-1 text-sm text-[#9297a1]">{word.meaning}</p></>; return linked ? <Link key={word.word} href={`/entry/${linked.id}`} className="rounded-xl bg-[#101b2b]/70 p-4 hover:bg-[#172434]">{content}<p className="mt-3 text-xs text-[#e5b85c]">Open word →</p></Link> : <div key={word.word} className="rounded-xl bg-[#101b2b]/70 p-4">{content}</div>; })}</div></div></>;
}

function GrammarDetail({ item, contrasts, vocabulary, kanji }: Readonly<{ item: GrammarItem; contrasts: GrammarContrast[]; vocabulary: VocabularyItem[]; kanji: KanjiItem[] }>) {
  const contrast = contrasts.find((entry) => item.contrastIds.includes(entry.id));
  return <><p className="jp-serif text-3xl text-[#e5b85c]"><JapaneseText text={item.pattern} vocabulary={vocabulary} kanji={kanji} /></p><h2 className="mt-5 text-xl font-medium">{item.meaning}</h2><p className="mt-3 text-sm leading-7 text-[#9297a1]">{item.formation}</p><p className="mt-5 text-sm leading-7 text-[#c3a998]">{item.intuition}</p><div className="mt-7"><p className="eyebrow mb-3">Usage conditions</p><ul className="space-y-2 text-sm leading-6 text-[#c3c7ce]">{item.usageConditions.map((condition) => <li key={condition}>— {condition}</li>)}</ul></div><div className="mt-7 space-y-3">{item.examples.map((example) => <div key={example.japanese} className="rounded-xl bg-[#101b2b]/70 p-4"><p className="jp-serif text-lg text-[#e5b85c]"><JapaneseText text={example.japanese} vocabulary={vocabulary} kanji={kanji} /></p><p className="mt-1 text-sm text-[#9297a1]">{example.translation}</p><GrammarVisual sentence={example.japanese} vocabulary={vocabulary} kanji={kanji} /><SaveSentence sourceItemId={item.id} japanese={example.japanese} translation={example.translation} /></div>)}</div><div className="mt-7 rounded-xl border border-[#713b37]/70 bg-[#21191a]/60 p-4"><p className="eyebrow mb-2">Common mistake</p><p className="text-sm leading-6 text-[#c3c7ce]">{item.commonMistakes.join(" ")}</p></div>{contrast ? <div className="mt-7 border-l-2 border-[#e34a3f] pl-4"><p className="eyebrow">Contrast · {contrast.title}</p><p className="mt-2 text-sm leading-6 text-[#c3a998]">{contrast.explanation}</p></div> : null}</>;
}

function OtherDetail({ item, vocabulary, kanji }: Readonly<{ item: ReadingItem | ListeningItem; vocabulary: VocabularyItem[]; kanji: KanjiItem[] }>) {
  if (item.category === "reading") return <ReadingPanel item={item} vocabulary={vocabulary} kanji={kanji} />;
  const playTranscript = (rate = 0.9) => { if (!("speechSynthesis" in window)) return; window.speechSynthesis.cancel(); const utterance = new SpeechSynthesisUtterance(item.transcript); utterance.lang = "ja-JP"; utterance.rate = rate; window.speechSynthesis.speak(utterance); };
  return <><p className="text-sm leading-7 text-[#9297a1]">{item.situation}</p>{item.audioUrl ? <audio controls src={item.audioUrl} className="mt-6 w-full" /> : <button type="button" onClick={() => playTranscript()} onDoubleClick={() => playTranscript(0.6)} title="Double-tap to replay slowly" className="mt-6 inline-flex min-h-12 items-center gap-3 rounded-xl border border-[#4f9ac0] bg-[#102536]/70 px-5 py-3 text-sm font-semibold text-[#d9eef8] hover:border-[#8cc9e5]"><span className="text-lg" aria-hidden="true">🔊</span> Play Japanese audio</button>}<p className="jp-serif mt-6 whitespace-pre-line text-lg leading-8 text-[#f5f5f2]"><JapaneseText text={item.transcript} vocabulary={vocabulary} kanji={kanji} /></p><p className="mt-6 text-sm text-[#9297a1]">{item.questions.length} listening questions ready in Practice.</p></>;
}

export function EntryDetail({ item, contrasts, vocabulary = n5Module.vocabulary, kanji = n5Module.kanji }: Readonly<{ item: Entry; contrasts: GrammarContrast[]; vocabulary?: VocabularyItem[]; kanji?: KanjiItem[] }>) {
  const detail = item.category === "vocabulary" ? <VocabularyDetail item={item} vocabulary={vocabulary} kanji={kanji} /> : item.category === "kanji" ? <KanjiDetail item={item} vocabulary={vocabulary} /> : item.category === "grammar" ? <GrammarDetail item={item} contrasts={contrasts} vocabulary={vocabulary} kanji={kanji} /> : <OtherDetail item={item} vocabulary={vocabulary} kanji={kanji} />;
  const assistantText = item.category === "vocabulary" ? item.exampleSentences[0]?.japanese : item.category === "grammar" ? item.examples[0]?.japanese : item.category === "reading" || item.category === "listening" ? item.category === "reading" ? item.passage : item.transcript : "";
  const classification = classifyItem(item);
  return <div className="mx-auto max-w-4xl"><div className="mb-6"><Link href="/library" className="text-sm text-[#e5b85c] hover:text-[#f1cf7c]">← Back to Library</Link></div><section className="surface-panel overflow-hidden p-7 sm:p-10"><div className="mb-8 flex items-start justify-between gap-4"><div><p className="eyebrow">{item.category} · {item.jlptLevel ?? "open"}</p><h1 className="mt-2 text-2xl font-medium text-[#f5f5f2]">{item.title}</h1><p className="mt-1 text-xs text-[#9297a1]">{item.subcategory ?? "N5 Foundations"} · difficulty {item.difficulty}</p></div><div className="flex flex-col items-end gap-3"><span className="seal" aria-label={`${item.jlptLevel ?? "Open"} entry`}><span>{item.jlptLevel ?? "—"}</span><small>道</small></span><StudyLaterButton itemId={item.id} /></div></div><div className="mb-7 flex flex-wrap gap-2 text-[10px] text-[#676c75]"><span className="rounded-md border border-white/10 px-2 py-1">{classification ? `${classification.band} · ${classification.confidence} confidence` : "unclassified"}</span>{item.sourceIds?.map((sourceId) => <span key={sourceId} className="rounded-md border border-white/10 px-2 py-1">source · {sourceId}</span>)}</div>{detail}<LearnerAssistant defaultText={assistantText} itemId={item.id} /><div className="mt-8 border-t border-white/10 pt-5"><Link href={`/practice?mode=${item.category}`} className="inline-flex rounded-xl bg-[#e34a3f] px-4 py-3 text-sm font-semibold text-[#0b0b0d] hover:bg-[#ef675d]">Practice {item.category} <span className="ml-2" aria-hidden="true">→</span></Link></div><ItemNote itemId={item.id} /></section></div>;
}

export type { Entry };
